---
description: Memory Bank — 跨会话项目上下文与决策记录
alwaysApply: true
---

# Memory Bank

**位置**：项目根 `MEMORY.md`，按时间倒序追加。

**读取**：不要主动读全文（文件很大）；仅在需要历史决策/架构上下文时，用 grep/分段读取相关条目。

**何时写**：重要里程碑/架构决策、用户要求、会话结束有未完成交接时。

**格式**：

```markdown
## YYYY-MM-DD — 简短标题

- **背景**：
- **决策/结论**：
- **待办/遗留**：
```

**原则**：只记有价值信息；决策写「为什么」；不确定标「待确认」，不臆测。
